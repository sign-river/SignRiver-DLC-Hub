# SteamOS / macOS ????????0.1.x ???

????? SignRiver DLC Hub ?? SteamOS?Linux?? macOS ??????????
????????????????????? `patch.platforms` ?? cartridge
??? Windows ?????

## ??

??????????

| ?? | ???????? | ??????? | ???? | ????? |
|---|---|---|---|---|
| Windows | `steam_api64.dll` | `steam_api64_o.dll` | `cream_api.ini` | CreamAPI ? dll |
| SteamOS | `libsteam_api.so` | `libsteam_api_o.so` | `SmokeAPI.config.json` | SmokeAPI v4.1.3 ? `libsmoke_api64.so`???? |
| macOS | `libsteam_api.dylib` | `libsteam_api_o.dylib` | `icecream.ini` | [krnya/icecream](https://github.com/krnya/icecream)?Rust ??????? |

- SmokeAPI ?????? DLC?`SmokeAPI.config.json` ??? AppInfo ?? DLC ??
  ?? `extra_dlcs`???????????/?? DLC????????
- icecream ? CreamAPI ????? `[dlc]` ?????????? `icecream.ini`?

## ?????????app/versions/0.1.0?

1. **domain/patches.py**
   - ?? `PatchPlatform`?windows / steamos / macos?? `PatchConfigFormat`?cream_ini / smokeapi_json??
   - ?? `host_patch_platform()`?? `sys.platform` ?????????
   - `PatchTemplate` ?? `config_format`?`PatchProfile` ?? `platform`?

2. **infrastructure/patching/engine.py**
   - ?? `render_smoke_api_config()`??? SmokeAPI v4 JSON ???
   - ?? `render_patch_config()`?? `config_format` ?????
   - `apply()` ? cream_ini ?? UTF-8 BOM?smokeapi_json ?? BOM?
   - ??????????????????? `MZ` ??? PE/ELF/Mach-O?

3. **domain/cartridges.py**
   - cartridge ??? `patch.platforms` ??????????/??/ini/config_format?
     ????????????? DLC ?????
   - `CartridgeDocument.patch_fields_for(platform)` ?????????`patch_platforms` ???????
   - `to_dict()` ?? platforms ????? round-trip?

4. **adapters/document_cartridge.py**
   - `build_cartridge_from_document(document, platform=None)`???????????
     ???? `"windows" / "steamos" / "macos"`?

5. **adapters/common/configured_steam.py**
   - `ConfiguredSteamAdapter` ?? `platform` ???descriptor ?????
   - ? Windows ???????????????????? `libsteam_api.so` /
     `libsteam_api.dylib` ?????????????? SteamOS/macOS ???????????

6. **config/cartridges/*.json**
   - 5 ??????? `patch.platforms`?steamos + macos????? `cartridges_index.json` ? sha256/size?

7. **tests/test_patch_platforms.py??? 12 ????**
   - SmokeAPI JSON ????????
   - ?????????????????????
   - ??????? profile?
   - SteamOS ?? profile ? apply/remove ???ELF ?????JSON ? BOM?????
   - ???? Windows ?????

???????`pytest`?445 ???

## ????

1. **?????**?`signriver-dlc-assets` ????????????
   - `libsteam_api.so`?SmokeAPI ?????????? `libsteam_api_o.so`?
   - macOS ? `libsteam_api.dylib`?icecream ??????????? `libsteam_api_o.dylib`?
   - AppInfo JSON ??????
   ???? profile ????????????????????????????????

2. **icecream dylib ??**????? release???? macOS?? GitHub Actions / cargo-zigbuild?
   ?? `libsteam_api.dylib`?Intel?Apple Silicon ??????????`cargo build --release`?

3. **????????????**????? HOI4/Stellaris ??? Linux/macOS ?????
   ?`hoi4` / `stellaris` ? `.app/Contents/MacOS/...`??????? SteamOS/macOS ?
   ????????????????? cartridge ? `patch.platforms.<platform>.executable_relative_path`?

4. **UI**??????????????????????????????????????
   ?? `cartridge.patch_platform`?

5. **Steam ????**?`SteamInstallationLocator` ???? Windows????/steamapps??
   SteamOS/macOS ???????????????? `~/.local/share/Steam/steamapps` ?
   `~/Library/Application Support/Steam/steamapps` ???
