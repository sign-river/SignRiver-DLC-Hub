# 执行状态

> 最后更新：2026-08-14

## 当前状态

Windows 客户端、稳定启动器、原地全量更新链路和配套发布器已基本收尾。当前正式版本为 `0.1.7`，Host API 为 `2`，活动模块为 `0.1.7`。

`0.1.7` 全量更新包及 GitLink/GitHub 双源清单已经生成，清单将其标记为强制更新；模块维护基线为 `0.1.5`、`0.1.6`、`0.1.7`。详细产物和版本状态见 `docs/current-progress.md`。

## 已完成范围

- Windows Steam 游戏发现、多游戏卡带、DLC 下载与内容寻址缓存。
- DLC 事务化安装、回执审计、保守修复和安全卸载。
- CreamAPI 补丁安装、健康检查、修复和原版恢复。
- 稳定启动器、模块更新、全量原地更新、失败回滚和多版本回退链。
- GitLink/GitHub 双下载源，以及公告、卡带、DLC 和程序更新的源联动。
- 发布器资源管理、模块归档、程序更新、双源镜像、上传进度及暂停/继续。
- 强制更新：启动后自动检查并静默重试，重要更新不可跳过。
- PyInstaller Windows 构建、UPX 压缩、发布清单和 CI 恢复历史模块归档。

跨平台补丁的数据模型与配置支持已经进入受控源码，但 Windows 仍是当前完成验收的主要交付平台。

## 当前验证

- `python -m pytest`：`510 passed`（2026-08-14）。
- `app/state.json`、启动器常量与模块元数据均为 `0.1.7`。
- `0.1.7` 双源更新清单的大小和 SHA-256 与本地全量包一致。
- 工作区不跟踪发布器私有配置、发布工作区、构建产物或测试基线。

## 发布约束

- 模块业务改动先写入 `app/versions/0.1.0/`，发布时同步到新的目标版本目录。
- 每次新版本构建都要更新 `publisher-workspace/update-notes.json`。
- 更新 ZIP 必须先上传并确认可下载，最后才能替换远端清单。
- 提交代码后默认不自动推送；只有用户明确要求时才能推送。
- `publisher-workspace/`、`publisher.local.json`、测试基线和令牌不得提交或上传。

## 后续方向

- Windows 端以维护、缺陷修复和新增游戏卡带为主。
- 跨平台补丁支持仍需在 SteamOS/macOS 真机完成端到端验证后才能视为正式交付。
- 新版本发布继续保留最近三个模块归档，并验证双源清单和回滚链。
