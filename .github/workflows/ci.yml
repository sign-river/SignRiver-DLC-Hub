name: CI

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-24.04, macos-15-intel]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      # 模块目录不入库：三个平台都要恢复已发布模块，否则依赖 0.2.0/1.0.0
      # 目录的用例只在 Windows 上能跑通。
      - run: python tools/restore_module_archives.py --source gitlink
      - run: python -m ruff check src tests tools app/versions/0.1.0
      - run: python -m pytest
        if: runner.os != 'Linux'
      - name: Run tests under Xvfb
        if: runner.os == 'Linux'
        run: xvfb-run -a python -m pytest
